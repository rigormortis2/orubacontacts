# Docker Ortamı için Environment Variables
# Bu dosyayı .env olarak kopyalayın: cp .env.docker .env

# Database Configuration (Docker Compose servis adları kullanılıyor)
DATABASE_URL="postgresql://postgres:postgres@database:5432/orubacontacts"

# Backend Configuration
PORT=3000
NODE_ENV=production

# Frontend Configuration (Geliştirme için - production'da Nginx kullanılıyor)
REACT_APP_API_URL=http://localhost:3000

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=orubacontacts
